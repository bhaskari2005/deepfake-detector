# Deepfake Image Detection — Ensemble of Pretrained Detectors
Upload an image -> runs through several pretrained detectors -> combined confidence score + summary.
Status: env ready (torch 2.2.1+cu121, A100 confirmed). Next: pick + wire up detectors.
